(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define(['module', 'exports', '../string/addZero'], factory);
  } else if (typeof exports !== "undefined") {
    factory(module, exports, require('../string/addZero'));
  } else {
    var mod = {
      exports: {}
    };
    factory(mod, mod.exports, global.addZero);
    global.getCurrentDay = mod.exports;
  }
})(this, function (module, exports, _addZero) {
  'use strict';

  Object.defineProperty(exports, "__esModule", {
    value: true
  });

  var _addZero2 = _interopRequireDefault(_addZero);

  function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {
      default: obj
    };
  }

  /**
   * 获取指定时间年月日
   * @memberof module:date
   * @param {Date} time 时间
   * @return {string} yyyy-mm-dd
   * @runkit true
   * @example
   * const getCurrentDay = require('zan-utils/date/getCurrentDay');
   * getCurrentDay(new Date());
   * // '2017-06-06'
   */
  function getCurrentDay(time) {
    time = time || new Date();
    var month = time.getMonth() + 1;
    month = (0, _addZero2['default'])(month);
    var day = time.getDate();
    day = (0, _addZero2['default'])(day);
    var year = time.getFullYear();
    return year + '-' + month + '-' + day;
  }
  exports['default'] = getCurrentDay;
  module.exports = exports['default'];
});
(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define(['module', 'exports'], factory);
  } else if (typeof exports !== "undefined") {
    factory(module, exports);
  } else {
    var mod = {
      exports: {}
    };
    factory(mod, mod.exports);
    global.hasFlash = mod.exports;
  }
})(this, function (module, exports) {
  'use strict';

  Object.defineProperty(exports, "__esModule", {
    value: true
  });
  /* global ActiveXObject */
  /**
   * 判断浏览器是否有flash
   * @memberof module:browser
   * @example hasFlash(); // false
   */
  function hasFlash() {
    var hasFlashPlayer = false;
    var xFlash = void 0;
    var plugins = navigator.plugins;
    var shockwave = navigator.mimeTypes && navigator.mimeTypes['application/x-shockwave-flash'];

    xFlash = function xFlash() {
      var result = false;
      var versions = ['ShockwaveFlash.ShockwaveFlash.6', 'ShockwaveFlash.ShockwaveFlash.7', 'ShockwaveFlash.ShockwaveFlash'];

      for (var i = 0; i < versions.length; i++) {
        if (!result) try {
          result = new ActiveXObject(versions[i]);
        } catch (e) {}
      }

      return !!result;
    };

    hasFlashPlayer = plugins && plugins.length && shockwave && shockwave.enabledPlugin || typeof ActiveXObject !== 'undefined' && xFlash() || false;

    return !!hasFlashPlayer;
  }
  exports['default'] = hasFlash;
  module.exports = exports['default'];
});